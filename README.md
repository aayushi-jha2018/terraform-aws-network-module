# terraform-aws-network-module
Reusable, tested Terraform module for provisioning n AWS VPC with public/private subnets, route tables,NAT, and security groups. Validated in CI via terrafrm fmt, validate, and plan (no real cloud credential).
